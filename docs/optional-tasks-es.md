# Tareas Opcionales

## Implementadas

- **Logging estructurado** — Logging estructurado con Pino configurado en toda la aplicación.

- **Documentación Swagger** — Documentación completa de la API disponible en `http://localhost:8000/docs`.

- **Paginación** *(parcial)* — Implementada en `GET /portfolios/`.

- **Filtros avanzados** *(parcial)* — Implementados en `GET /portfolios/{portfolioId}/transactions/` y `GET /portfolios/{portfolioId}`.

## No Implementadas

Las siguientes tareas opcionales no fueron completadas por falta de tiempo:

- **Tests unitarios y de integración**
- **Deployment en una plataforma**
